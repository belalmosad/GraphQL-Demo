
type Post {
    id: Int,
    title: String,
    content: String
}

type Query {
    post(id: Int): Post,
    posts: [Post]
}

type Mutation {
    addPost(id: Int, title: String, content: String): Post,
    deletePost(id: Int): [Post]
}